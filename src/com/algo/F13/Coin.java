package com.algo.F13;

/**
 * Created by timothy on 2017-03-05, com.algo.F13.
 */
public class Coin {
   /* public static int min(int[] coins, int change) {
        if (coins[change] != 0)
            return 1;

        int min = Integer.MAX_VALUE;
        int left;
        int coinsInReturn;
        for (int i = 1; i < change; i++) {
            if (coins[i] != 0) {
                left = change % coins[i];
                if (left > 0) {
                    coinsInReturn = change / coins[i];
                    if(min - coinsInReturn != 1)
                        for(int j = left; j > 0 && coinsInReturn < min; j--)

                }
                else

            }
        }*/

        /*
        kvar = växel % mynt
        min = växel / mynt

        om något kvar, loopa
         */

    //}

}
